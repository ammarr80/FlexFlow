import { CheckCheck, CircleCheckBig, createIcons, TrendingUp } from "lucide";

document.addEventListener("DOMContentLoaded", () => {
  createIcons({
    icons: {
      TrendingUp, CheckCheck, CircleCheckBig, Clock3
    },
  });
});
